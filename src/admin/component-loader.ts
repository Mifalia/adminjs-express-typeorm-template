import { ComponentLoader } from 'adminjs';

const componentLoader = new ComponentLoader();

const customComponents = {
  // use componentLoader.add() to register components
};

export { customComponents };
export default componentLoader;
